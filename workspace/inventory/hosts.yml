# The fleet you are defending. Pre-registered for you — by now you can build an
# inventory in your sleep; this mission is about defending it under fire.
all:
  children:
    fleet:
      hosts:
        sdc-app:
          ansible_host: localhost
          ansible_port: 2221
        sdc-web:
          ansible_host: localhost
          ansible_port: 2222
        sdc-db:
          ansible_host: localhost
          ansible_port: 2223
